name: Advanced Deploy to Render

on:
  push:
    branches: [ "main" ] # Đổi thành "master" nếu bạn dùng master

jobs:
  deploy:
    name: Build & Poll Render Status
    runs-on: ubuntu-latest
    permissions:
      deployments: write
      contents: read

    steps:
      - name: Start GitHub Deployment
        uses: bobheadxi/deployments@v1
        id: deployment
        with:
          step: start
          token: ${{ secrets.GITHUB_TOKEN }}
          env: production

      - name: Trigger Render Deploy
        id: trigger
        run: |
          # Gọi Deploy Hook và lấy mã Deploy ID từ phản hồi (nếu có) hoặc bắt đầu theo dõi
          curl -s -X POST "${{ secrets.RENDER_DEPLOY_HOOK }}"
          echo "Deploy triggered successfully."

      - name: Wait for Render to go Live
        run: |
          # Vòng lặp kiểm tra trạng thái (Polling)
          while true; do
            # Lấy thông tin bản deploy mới nhất từ API
            STATUS=$(curl -s -H "Authorization: Bearer ${{ secrets.RENDER_API_KEY }}" \
              "https://api.render.com/v1/services/${{ secrets.RENDER_SERVICE_ID }}/deploys?limit=1" \
              | jq -r '.[0].deploy.status')
            
            echo "Current Render Status: $STATUS"

            if [ "$STATUS" == "live" ]; then
              echo "Deploy is LIVE!"
              exit 0
            elif [ "$STATUS" == "build_failed" ] || [ "$STATUS" == "update_failed" ] || [ "$STATUS" == "canceled" ]; then
              echo "Deploy FAILED with status: $STATUS"
              exit 1
            fi

            echo "Still building... waiting 30 seconds."
            sleep 30
          done

      - name: Update GitHub Deployment Status
        uses: bobheadxi/deployments@v1
        if: always()
        with:
          step: finish
          token: ${{ secrets.GITHUB_TOKEN }}
          status: ${{ job.status }}
          env: production
          deployment_id: ${{ steps.deployment.outputs.deployment_id }}
          env_url: 'https://fpt-shop.onrender.com'